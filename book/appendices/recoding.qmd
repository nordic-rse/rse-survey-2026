# Recoding {#sec-recoding}

This appendix lists the free-text answers in this book. Each row gives the
category of one raw answer:

- **Unchanged**: no rule matches. The answer is its own category.
- **Recoded**: a rule assigns the answer to a category.
- **Excluded**: a rule removes the answer from the analysis.

The answers are split at commas. Identical answers from different respondents
count once. So `n` counts answer parts, not respondents.

{{< include ../_setup.qmd >}}

## What would you hope to get out of such an organisation (check all that

```{python}
#| output: asis
print(allocation_table(df, codebook, "org2can"))
```

## How could Nordic-RSE or a potential Nordic research software institute foster community engagement and knowledge

```{python}
#| output: asis
print(allocation_table(df, codebook, "org3nord"))
```

## With Nordic-RSE we have an association dedicated to the community of Research Software Engineers in the Nordics. What tasks do you think should be undertaken by Nordic-RSE or a future Nordic institute for research

```{python}
#| output: asis
print(allocation_table(df, codebook, "org4nord"))
```

## Where was your previous job based?

```{python}
#| output: asis
print(allocation_table(df, codebook, "prevEmp1"))
```

## What is the nature of your current employment?

```{python}
#| output: asis
print(allocation_table(df, codebook, "currentEmp10"))
```

## Please select the discipline(s) in which you work. Please select all that

```{python}
#| output: asis
print(allocation_table(df, codebook, "currentEmp13"))
```

## Who are you writing code for, supporting with your code or providing services

```{python}
#| output: asis
print(allocation_table(df, codebook, "currentWork2qcl"))
```

## At which conference(s)/workshop(s) have you presented your software work?

```{python}
#| output: asis
print(allocation_table(df, codebook, "conf2can"))
```

## How do you test the software that you produce? Please check all that

```{python}
#| output: asis
print(allocation_table(df, codebook, "proj4can"))
```

## Which collaboration tools do you use for software

```{python}
#| output: asis
print(allocation_table(df, codebook, "proj6zaf"))
```

## What training programs are you involved with?

```{python}
#| output: asis
print(allocation_table(df, codebook, "train3"))
```

## In what capacity do you

```{python}
#| output: asis
print(allocation_table(df, codebook, "train5"))
```

## Which of the following sources are used to pay for your effort as a Research Software

```{python}
#| output: asis
print(allocation_table(df, codebook, "fund3"))
```

## On which platform(s) is your most recent research software project deployed? Please select all that

```{python}
#| output: asis
print(allocation_table(df, codebook, "tool5"))
```

## Which operating system do you primarily use for development?

```{python}
#| output: asis
print(allocation_table(df, codebook, "tool2"))
```

## What programming languages do you use at work? Please select all that

```{python}
#| output: asis
print(allocation_table(df, codebook, "tool4can"))
```

## How did you learn the skills you need to become a Research Software Engineer / Research Software

```{python}
#| output: asis
print(allocation_table(df, codebook, "ukrse3"))
```

## What three skills would you like to acquire or improve to help your work as a Research Software Engineer / Research Software Developer? The skills can be technical and

```{python}
#| output: asis
print(allocation_table(df, codebook, "skill2"))
```

## For what purposes do you use generative AI

```{python}
#| output: asis
print(allocation_table(df, codebook, "genAI2"))
```

## Which generative AI tools do you currently use in your RSE

```{python}
#| output: asis
print(allocation_table(df, codebook, "genAI3"))
```
