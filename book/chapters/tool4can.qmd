---
title: "What programming languages do you use at work? Please select all that"
question_id: tool4can
---

{{< include ../_setup.qmd >}}

## Nordics

```{python}
plot_choice(df, codebook, "tool4can").show()
```

## By age group

```{python}
plot_choice(df, codebook, "tool4can", group_by="age_group").show()
```

## Within Nordics

```{python}
plot_choice(df, codebook, "tool4can", group_by="country").show()
```

## Between countries

```{python}
plot_choice(df_compare, codebook, "tool4can", group_by="country_group").show()
```
