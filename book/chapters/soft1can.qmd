---
title: "How many years of software development experience do you have?"
question_id: soft1can
---

{{< include ../_setup.qmd >}}

## Nordics

```{python}
plot_choice(df, codebook, "soft1can").show()
```

## By age group

```{python}
plot_choice(df, codebook, "soft1can", group_by="age_group").show()
```

## Within Nordics

```{python}
plot_choice(df, codebook, "soft1can", group_by="country").show()
```

## Between countries

```{python}
plot_choice(df_compare, codebook, "soft1can", group_by="country_group").show()
```
